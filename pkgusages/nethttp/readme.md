How to run and test this plg?

1. change the pkg name to main.
2. go run nethttp.go
3. curl -v -d "test curl" http://localhost:9010 

After the 3 step, you will see it receive the "test curl" in server endpoint.